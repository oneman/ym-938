/*C
 * *
 * */
#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
/*  int ret = 0;
  u8 page[4096];
  for (;;) {
    ret = read(STDIN_FILENO, page, sizeof(page));
    if (ret == 0) break;
    expand(page, ret);
  }*/
  return 0;
}
